# Test environment configuration
# This file is sourced by test scripts

# Mock API Keys (these are safe dummy values for testing)
export NVIDIA_KEY="test-key-12345"
export TELEGRAM_BOT_TOKEN="test-bot-token"
export TELEGRAM_CHAT_ID="test-chat-id"

# Test file paths
export STATE_FILE="./tests/test_trade_state.json"
export TRADE_HISTORY_FILE="./tests/test_trade_history.json"
export PREV_BRIEF_FILE="./tests/test_last_brief.txt"
export LOG_FILE="./tests/test_plumbus.log"

# Test API URLs (these should point to mock endpoints or be disabled)
export NVIDIA_MODEL="meta-llama/Llama-3.3-70B-Instruct"
export NVIDIA_TEMPERATURE="0.6"
export NVIDIA_MAX_TOKENS="4096"

# Timeout values for testing (shorter times for faster tests)
export API_TIMEOUT="5"
export NVIDIA_TIMEOUT="10"
export TELEGRAM_TIMEOUT="5"

# Retry settings for testing
export RETRY_MAX="1"
export RETRY_DELAY="1"

# Min volume filter
export MIN_VOLUME="10000"
